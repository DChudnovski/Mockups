The memo UI has many fixes that need to be implemented:
- Create Memo Modal: Is the modal to distinguish where the memo is being created from necessary? Can we just create memos from the Pending Memo list?
- Create Memo From Dashboard: A direct place to create a new memo from the dashboard - a button next to the Recent Memos
- Company Name Field: On fullscreen Desktop Window the Company Name field in "Customer" section is too short for some reason, Should be made longer - On smaller window its longer; why?
- New Layout for List: Intially populated with a single row with a (+ New) Button that will open a modal for a new item in the Memo
- New "+ New" Interface: Merge the Barcode scanning interface with the individual item interface. Have the dropdown option beneath the long field for inidividual numbers. If one has an input grey out the other.
- Further Questions: Is the Orders section necessary? How do we want users to interface with their Memo history? We should have the interface to search both open and closed memos from the same list; we currently do not have that capability. Do we want to relegate closed memos to a seperate list?
